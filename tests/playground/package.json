{
  "name": "cloudflare-email-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "wp-playground based e2e/integration tests for the Cloudflare Email plugin. Isolated from the root package so its heavy deps (@wp-playground/cli, playwright-core) never touch the Nix build's package-lock.json.",
  "scripts": {
    "test:assets": "node run-assets.mjs",
    "test:e2e": "node run-e2e.mjs",
    "test:browser": "node run-browser.mjs",
    "test": "node run-assets.mjs && node run-e2e.mjs"
  },
  "devDependencies": {
    "@wp-playground/cli": "^3.1.44",
    "playwright-core": "^1.49.0"
  }
}
