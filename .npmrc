; @wordpress/dataviews declares a React 18 peer, but we run the React 19 types
; (WordPress 7+). dataviews is bundled and its internal types are skipLibCheck'd,
; so accept the newer peers rather than pin back.
legacy-peer-deps=true
