vendor
node_modules
build
.devenv
.direnv
result*
cloudflare-email.zip
# Symlink generated by git-hooks.nix on each devShell entry; points at a
# system/hooks-hash-specific /nix/store path, so it must stay untracked.
.pre-commit-config.yaml
